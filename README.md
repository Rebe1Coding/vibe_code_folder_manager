# 🚀 Project Structure Generator

Быстрое и удобное приложение для создания структуры проекта на основе markdown-формата, полученного от LLM.

## 📋 О проекте

Любишь повайбкодить, дружок? Эта утилита для тебя.При разработке приложений с помощью LLM (ChatGPT, Claude и т.д.) разработчики часто получают готовую структуру проекта, но создавать все файлы и папки вручную - это скучно и отнимает время.

**Project Structure Generator** решает эту проблему:

1. ✅ **Просите у LLM структуру проекта** - LLM выдаст её в стандартном markdown формате
2. ✅ **Запускаете приложение** - стартует красивый интерфейс
3. ✅ **Вставляете структуру** - копируете текст от LLM
4. ✅ **Нажимаете кнопку** - приложение создаёт всю структуру за секунды!

## ⚡ Преимущества

- 🎯 **Экономия времени** - вместо ручного создания файлов один клик
- 🎨 **Современный интерфейс** - удобный и красивый UI на Flet
- 📊 **Детальное логирование** - видите что создано и где
- 🛡️ **Обработка ошибок** - приложение продолжает работу даже при проблемах
- ⚙️ **Гибкость** - поддерживает любые структуры проектов

## ⚠️ Важно знать

Приложение иногда может допускать ошибки при парсинге сложных структур, но даже с ошибками **значительно ускоряет работу**:
- Создаст большинство структуры корректно
- Покажет в логе какие элементы не удалось создать
- Вы легко доделаете остальное вручную за минуты вместо часов

## 📦 Установка

### Требования
- Python 3.8+
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте или скачайте проект**
```bash
git clone <url-репозитория>
cd vibe_code_folder_manager
```

2. **Установите зависимости**
```bash
python -m venv venv
source venv/bin/activate  # или venv\Scripts\activate на Windows
pip install -r requirements.txt
```

3. **Запустите приложение**
```bash
python main.py
```

Приложение откроется в новом окне!

## 🎯 Как использовать

### Пример использования

1. **В чате с LLM запросите структуру проекта:**
```
Создай структуру проекта для веб-приложения на Flask с следующей архитектурой:
- папка для шаблонов HTML
- папка для статических файлов (CSS, JS, images)
- файлы конфигурации
- структура для роутов
```

2. **Скопируйте структуру, которую вернул LLM:**
```
my_flask_app/
├── README.md
├── requirements.txt
├── config.py
├── app.py
├── templates/
│   ├── base.html
│   ├── index.html
│   └── dashboard.html
├── static/
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── script.js
│   └── images/
├── routes/
│   ├── __init__.py
│   ├── auth.py
│   ├── dashboard.py
│   └── api.py
└── tests/
    ├── __init__.py
    ├── test_auth.py
    └── test_routes.py
```

3. **Откройте приложение и:**
   - Нажмите **"Выбрать папку"** и выберите где создавать проект
   - Вставьте структуру в поле **"Структура проекта"**
   - Нажмите **"Создать структуру проекта"**
   - Готово! 🎉 Структура создана в выбранной папке

## 🏗️ Форматы структуры

Приложение поддерживает структуры в формате, который генерируют LLM:

```
project-name/
├── folder1/
│   ├── file1.txt
│   └── file2.py
├── folder2/
│   └── subfolder/
│       └── file3.js
└── file4.html
```

### Правила форматирования

- **Корневая папка** - первая строка (будет создана в выбранном месте)
- **Папки** - заканчиваются на `/`
- **Файлы** - без слэша в конце
- **Отступы** - обозначают вложенность (4 пробела = 1 уровень)
- **Символы дерева** - `├──`, `└──`, `│` (любые комбинации поддерживаются)
- **Комментарии** - всё после `#` игнорируется

## 📝 Примеры команд для LLM

### Пример 1: React приложение
```
Создай структуру React приложения с использованием:
- Create React App структурой
- папкой components
- папкой pages  
- папкой hooks
- папкой utils
```

### Пример 2: Django проект
```
Сгенерируй структуру Django проекта с моделями, вьюхами и тестами
```

### Пример 3: Python пакет
```
Создай структуру Python пакета с setup.py, документацией и примерами
```

## 🐛 Что делать если произошла ошибка?

1. **Проверьте структуру** - убедитесь что формат правильный
2. **Посмотрите лог** - там будет информация о конкретной ошибке
3. **Обработайте вручную** - доделайте ошибочные файлы/папки самостоятельно
4. **Используйте фрагменты** - если большая структура вызывает ошибки, разбейте её на части

## 📂 Структура проекта

```
vibe_code_folder_manager/
├── main.py          # Точка входа приложения
├── ui.py            # Интерфейс приложения (Flet)
├── parser.py        # Парсер структуры и создатель файлов
├── requirements.txt # Зависимости
└── README.md        # Этот файл
```

## 🔧 Технические детали

- **UI Framework:** Flet
- **Язык:** Python 3.8+
- **Основной модуль парсирования:** `parser.py`
  - `StructureParser` - парсит markdown структуру
  - `StructureCreator` - создаёт файлы и папки на диске

## 📄 Лицензия

Проект создан для ускорения разработки с использованием LLM.

